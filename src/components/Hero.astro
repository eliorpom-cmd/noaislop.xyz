---
interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<div class="hero">
  <h1 class="name">{title}</h1>
  <span class="subtitle">{subtitle}</span>
</div>
