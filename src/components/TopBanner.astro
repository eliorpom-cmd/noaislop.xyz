---
// One line above the title: the language question on the left, the menu on the
// right, and a hairline under both to close the block.
import LanguageSwitcher from './LanguageSwitcher.astro';

interface Props {
  locale: string;
  prompt: string;
  switcherLabel: string;
}

const { locale, prompt, switcherLabel } = Astro.props;
---

<div class="doc-meta container">
  <span>{prompt}</span>
  <LanguageSwitcher locale={locale} label={switcherLabel} />
</div>

<style>
  .doc-meta {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.25rem 1rem;
    margin-top: 2rem;
    padding-bottom: 0.85rem;
    border-bottom: 1px solid var(--rule);
    font-size: 0.9375rem;
    color: var(--muted);
  }

  @media (max-width: 30rem) {
    .doc-meta {
      margin-top: 1.25rem;
      font-size: 0.875rem;
    }
  }
</style>
