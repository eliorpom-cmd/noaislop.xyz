---
interface Props {
  quote: string;
}

const { quote } = Astro.props;
---

<p class="lead pull">{quote}</p>

<style>
  .pull {
    margin: 0 0 120px;
    max-width: 900px;
  }

  @media (max-width: 768px) {
    .pull {
      margin-bottom: 70px;
    }
  }
</style>
